server {
       listen 80;
       listen [::]:80;

       server_name agungkristiawan.my.id;

       root /var/www/hrmsanur.my.id;
       index index.html;

       location / {
               try_files $uri $uri/ =404;
       }
}